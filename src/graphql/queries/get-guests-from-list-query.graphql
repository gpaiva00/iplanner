query GetGuestsFromList($id: ID = "") {
  guestList(where: { id: $id }) {
    id
    listName
    items(last: 10000000) {
      ... on Guest {
        id
        name
        confirmationCode
        phone
        response
      }
    }
  }
}
